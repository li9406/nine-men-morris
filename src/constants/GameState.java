package constants;

/**
 * An enum class that represents the possible states of the game.
 * The enum values provide a convenient way to represent and handle different game states throughout the game.
 */
public enum GameState {
    PLACE, REMOVE, SLIDE, JUMP, GAMEOVER, DRAW
}
