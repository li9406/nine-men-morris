package components;

/**
 * A enum class that represents the possible colors that a team can have: WHITE or BLACK
 * Each team can only have one color.
 */
public enum Team {
    WHITE, BLACK
}
